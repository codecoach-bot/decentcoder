"use strict";
/*
const num1 = 5;
const num2 = 5.5;

function add(n1: number , n2: number){
return n1+n2;

}
// In ts we defined the type of variable at compile time
//if type id not sutaible it showing the CTE;
// that why use the ts

const result = add(num1, num2);
console.log(result);
//document.write(result);

const person:{
    name: string,
    age: number,
    address: string,
    hobby: string[],
    role: [number, string]
} = {
    name : "kiran",
    age: 27,
    address: "kingaon",
    hobby: ['cricket', 'hoocky', 'football'],
    role: [1, 'yash']
};

let favActivities: string[];
favActivities = ['sports'];

for(const boy of person.hobby){
console.log(boy.toUpperCase());
}
*/
/*
function add(num1: number, num2: number){
return num1+num2;
}

function sum(result: number) {
    console.log('result is: '+ result);
}
sum(add(5,5));

const name1: string[] = [];~

name1.push("kiran");
name1.push("yash");
// name1.push(5); // argument of type number is not assignabble to string type.

// tsc file.ts --watch is compile the file automatically and recopile if any changes happen in that ....and showing the result.
*/
console.log("hello");
